#!/usr/bin/env bash
set -euo pipefail

uv run --python 3.11 --with ruff --no-project ruff format --check lightwatch tests
uv run --python 3.11 --with ruff --no-project ruff check lightwatch tests
PYTHONPATH=. uv run --python 3.11 --with pytest --no-project pytest -q
